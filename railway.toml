[build]
builder = "RAILPACK"
buildCommand = "npm ci && npm run build"

[deploy]
preDeployCommand = "./scripts/railway-predeploy.sh"
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
